#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../x-default-browser/bin/x-default-browser.js" "$@"
  ret=$?
else 
  node  "$basedir/../x-default-browser/bin/x-default-browser.js" "$@"
  ret=$?
fi
exit $ret
