{
    "debugMode": true,
    "handlers": {
        "^/api/": "$./handlers/restful"
    },
    "restful": {
        "path": "./api",
        "routes": [
            {
                "pattern": "/api/hello/{name}",
                "target": "./hello"
            }
        ]
    }
}