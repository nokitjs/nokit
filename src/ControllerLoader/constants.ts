/**
 * 控制器基本信息 metadataKey
 */
export const CTL_INFO = Symbol("CTL_INFO");

/**
 * 控制器路由映射信息 metadataKey
 */
export const CTL_MAPPING = Symbol("CTL_MAPPING");

/**
 * 控制器参数注入信息 metadataKey
 */
export const CTL_PARAMETER = Symbol("CTL_PARAMETER");
